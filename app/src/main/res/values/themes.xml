<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AdoptMe" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brandGrey</item>
        <item name="colorPrimaryVariant">@color/brandPurple</item>
        <item name="colorOnPrimary">@color/brandOrange</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/brandOrange</item>
        <item name="colorSecondaryVariant">@color/brandPurple</item>
        <item name="colorOnSecondary">@color/brandGrey</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.AdoptMe.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AdoptMe.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AdoptMe.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>